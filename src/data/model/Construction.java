package data.model;

import org.newdawn.slick.opengl.Texture;

public class Construction extends Placeable {
    public Construction(Texture texture, String name, float x, float y, double width, double height, double length) {
        super(texture, name, x, y, width, height, length);
    }
}
