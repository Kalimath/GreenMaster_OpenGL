package data.model;

import org.newdawn.slick.opengl.Texture;

public class Construction extends Placable {
    public Construction(Texture texture, String name, float x, float y, int width, int height) {
        super(texture, name, x, y, width, height);
    }
}
