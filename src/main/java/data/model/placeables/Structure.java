package data.model.placeables;

public class Structure extends Placeable {
    public Structure(String name, double width, double height, double length, Type type, String texturePath) {
        super(name, width, height, length, type, texturePath);
    }
}
